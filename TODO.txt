Find out why my DateConverter doesn't display dates correctly (at all).
	It gets instantiated but not called!!

Finish the SyncAdapter for two-way synch.

Add a "Test" button in Prefs for account and server info.

Implement two-location delete algorithm.

Use CursorLoader to sync dynamically with changes in Local DB

Then call it quits for now... work on other stuff.

And, BTW, move this list into a TodoMore database(!).
